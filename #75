Let's solve the LeetCode problem "Sort Colors" (#75) 

class Solution {
    public void sortColors(int[] nums) {
        int low = 0, mid = 0;
        int high = nums.length - 1;
        
        while (mid <= high) {
            if (nums[mid] == 0) {
                // Swap nums[low] and nums[mid]
                int temp = nums[low];
                nums[low] = nums[mid];
                nums[mid] = temp;
                
                low++;
                mid++;
            } else if (nums[mid] == 1) {
                // Just move forward when it's 1
                mid++;
            } else {
                // nums[mid] == 2
                // Swap nums[mid] and nums[high]
                int temp = nums[mid];
                nums[mid] = nums[high];
                nums[high] = temp;
                
                high--;
            }
        }
    }
}
Why use it here?

The input is already given as an array.
We can exploit the properties of arrays (random access, constant-time updates) to efficiently swap elements in-place.
No need for extra complex data structures, which makes the solution space efficient.
